[project]
name = "git-permalink-fixer"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "huyz", email = "huyz@users.noreply.github.com" }
]
dependencies = [
    "requests>=2.32.3",
]
readme = "README.md"
requires-python = ">= 3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/git_permalink_checker"]
